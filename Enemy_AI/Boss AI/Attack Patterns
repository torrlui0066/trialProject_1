var attack_hitbox = $AttackHitbox
var attack_cooldown = 2  # Adjust cooldown as needed
var time_since_last_attack = 0

func _physics_process(delta):
    time_since_last_attack += delta

    # Example: Attack when player is within a certain range
    if player_position.distance_to(position) < 200 and time_since_last_attack > attack_cooldown:
        perform_attack()
        time_since_last_attack = 0

func perform_attack():
    # Example: Show animation and apply damage to player
    $Sprite.play("attack_animation")
    attack_hitbox.visible = true
    # Add logic to apply damage to player within the hitbox
